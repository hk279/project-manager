import { Spin } from "antd";

const Loading = () => {
    return (
        <div className="loading-spinner-container">
            <Spin size="large" />
        </div>
    );
};

export default Loading;
